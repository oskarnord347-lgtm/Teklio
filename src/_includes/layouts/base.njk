{# src/_includes/layouts/base.njk #}
<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>
    {% if title %}
      {{ title }} | Teklio
    {% else %}
      Teklio – einfache Hilfe bei Technikproblemen
    {% endif %}
  </title>

  {% if description %}
    <meta name="description" content="{{ description | safe }}" />
  {% else %}
    <meta name="description" content="Einfache Schritt-für-Schritt Anleitungen für Fernseher, Handy und Computer – besonders verständlich für Einsteiger." />
  {% endif %}

  {% if canonical %}
    <link rel="canonical" href="{{ canonical }}" />
  {% endif %}

  <meta property="og:site_name" content="Teklio" />
  <meta property="og:type" content="website" />
  <meta property="og:title" content="{% if title %}{{ title }} | Teklio{% else %}Teklio{% endif %}" />
  <meta property="og:description" content="{% if description %}{{ description | safe }}{% else %}Einfache Schritt-für-Schritt Technik-Anleitungen.{% endif %}" />

  <link rel="stylesheet" href="/assets/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>
  <a class="skip-link" href="#content">Zum Inhalt springen</a>

  <header class="site-header">
    <div class="wrap">
      <a class="brand" href="/">Teklio</a>
      <nav class="site-nav" aria-label="Hauptnavigation">
        <a href="/assistent/">Assistent</a>
        <a href="/">Suche</a>
      </nav>
    </div>
    <div class="theme-menu" aria-label="Theme">
  <button class="theme-btn" type="button" id="themeBtn" aria-haspopup="true" aria-expanded="false">
    <span class="theme-icon" aria-hidden="true">🌓</span>
    <span class="theme-label">Theme</span>
  </button>

  <div class="theme-popover" id="themePopover" role="menu" aria-label="Theme auswählen">
    <button type="button" class="theme-option" data-theme-choice="system" role="menuitem">System</button>
    <button type="button" class="theme-option" data-theme-choice="light" role="menuitem">Hell</button>
    <button type="button" class="theme-option" data-theme-choice="dark" role="menuitem">Dunkel</button>
  </div>
  </div>

  </header>

  {# Breadcrumbs für Guide-Seiten (alias: guide) #}
  {% if guide %}
    <nav class="breadcrumbs wrap" aria-label="Breadcrumb">
      <ol>
        <li><a href="/">Start</a></li>
        <li><a href="{{ guide.deviceUrl }}">{{ guide.deviceName }}</a></li>
        <li><a href="{{ guide.brandUrl }}">{{ guide.brandName }}</a></li>
        <li><a href="{{ guide.url }}">{{ guide.issueName }}</a></li>
      </ol>
    </nav>
  {% endif %}

  <main id="content" class="wrap">
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="wrap">
      <small>
        © {{ "now" | date("yyyy") }} Teklio ·
        <a href="/impressum/">Impressum</a> ·
        <a href="/datenschutz/">Datenschutz</a>
      </small>
    </div>
  </footer>
  <script src="/assets/script.js" defer></script>

</body>
</html>
