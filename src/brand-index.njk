---
layout: layouts/base.njk
pagination:
  data: brandIndexPages
  size: 1
  alias: brand
permalink: "{{ brand.url }}index.html"
title: "{{ brand.deviceName }} – {{ brand.brandName }}"
description: "Anleitungen für {{ brand.deviceName }} ({{ brand.brandName }}) – Schritt für Schritt, einfach erklärt."
canonical: "https://teklio.de{{ brand.url }}"
breadcrumbs:
  - { name: "Start", url: "/" }
  - { name: "{{ brand.deviceName }}", url: "/{{ brand.deviceSlug }}/" }
  - { name: "{{ brand.brandName }}", url: "{{ brand.url }}" }
---

<h1>{{ brand.brandName }} – {{ brand.deviceName }}</h1>

{% if brand.system %}
  <p><strong>System:</strong> {{ brand.system }}</p>
{% endif %}

<h2>Probleme</h2>
<ul>
  {% for i in brand.issues %}
    <li><a href="{{ i.url }}">{{ i.issueName }}</a></li>
  {% endfor %}
</ul>

<p style="margin-top: 16px;">
  <a href="/assistent/">Zum Assistenten</a>
</p>
